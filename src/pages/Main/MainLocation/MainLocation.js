import React from 'react';
import styled from 'styled-components';

const MainLocation = () => {
  return <MainLocationContainer>메인로케이션</MainLocationContainer>;
};

const MainLocationContainer = styled.section``;

export default MainLocation;
